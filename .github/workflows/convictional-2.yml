name: Test Workflow

on:
  workflow_dispatch:
  push:
   branches: [ convictional-2 ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.client_payload.ref }}
      - name: Download build artifacts
        uses: actions/download-artifact@v2
        with:
          name: build-artifacts
          path: .
          repository: Mayureshgithub/upload_artifact_private_repository     # Replace with the owner and repository name of Repository A
          ref: ${{ github.event.release.tag_name }}
          token: ${{ secrets.PAT_TOKEN_TEST }}
      - name: Test
        run: |
          ls -lrta 
          # test commands here

#    deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     steps:
#       - name: Download build artifacts
#         uses: actions/download-artifact@v2
#         with:
#           name: build-artifacts
#           repository: owner/repo-name   # Replace with the owner and repository name where the artifacts were uploaded
#           path: .
