name: Test Workflow

on:
  repository_dispatch:
    types: [trigger-workflow_event]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.client_payload.ref }}
      - name: Download build artifacts
        uses: actions/download-artifact@v2
        with:
          name: build-artifacts
          path: .
      - name: Test
        run: |
          ls -lrta 
          # test commands here
